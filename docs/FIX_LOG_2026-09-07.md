# 2026-09-07 고친 것 기록

브랜치 `feat/menu-ia`. 마지막 커밋 `248f5ed` 위에 얹은 워킹 트리 변경분이다.
전부 Playwright 로 실제 브라우저에 띄워서 `getBoundingClientRect` / `getComputedStyle` 로 쟀다.
스크린샷 눈대중은 쓰지 않았다.

---

## 1. 이번에 새로 찾아서 고친 것

| # | 어디 | 무슨 일이 있었나 | 왜 그랬나 | 어떻게 고쳤나 | 잰 값 |
|---|---|---|---|---|---|
| 1 | 홈 「메리디안의 약속」 (웹) | 답하는 두 마디가 화면 밑으로 잘려 나갔다.<br>「기준이 되어주어야 할 세무대리인이…」<br>「메리디안은 그 구조 자체를 다르게 두기로 했습니다」 | 붙는 판이 `100svh` 인데 그 안에 들어갈 것이 더 많았다.<br>위 여백 152 + 제목 185 + 사이 36 + 대화 560 = **933px**.<br>1280×720 화면에서 판은 720px 뿐이라 213px 이 밖으로 나갔다.<br>약속 원도 판 바닥에 딱 붙어 여유가 0 이었다. | `globals.css` 에 낮은 화면용 규칙 두 겹을 **새로 붙였다**(기존 규칙은 안 건드림).<br>≤940px: 위 여백 152→116, 제목 아래 여백 축소<br>≤820px: 위 여백 152→84, 말풍선 사이 간격·사람 그림(205→128px)·말풍선 글자 축소<br>≥901px 조건을 걸어 휴대폰(쌓는 판)은 제외 | 1280×720 : 213px 모자람 → **107px 남음**<br>1440×800 : 잘림 → **잘림 0**<br>1440×900 : 3px 모자람 → **78px 남음**<br>1920×1080 : 변화 없음 |
| 2 | 헤더 유리 굴절 | 글이 헤더 밑을 지날 때 한 번 불룩했다가 튕겨 나왔다. 물방울처럼 보였다. | 아랫입술 변위 지도가 `128 → 240 → 128` 로 **갔다 돌아왔다**. 띠 안에서 밀었다가 되돌리니 렌즈가 됐다.<br>그리고 그 전부를 10px 안에서 처리해 1px 갈 때마다 0.62px 씩 꺾였다. | ① 띠 길이 10px → **20px**<br>② 세기 `scale` 14 → **10** (70%)<br>③ 아랫입술 지도를 `128 → 240` 로 올린 뒤 **그대로 유지**. 유리가 끝나는 자리에서 밀린 채 끊긴다 — 그게 유리 모서리다.<br>좌우 띠는 그대로 `128 → 최고 → 128`. 안 그러면 페이지가 통째로 옆으로 밀린다. | 띠 56.4~77.0px (**20.6px**)<br>최대 밀림 6.18 → **4.41px** (71%)<br>기울기 0.62 → **0.244 px/px** (39%)<br>휴대폰용 2.2 → **1.54px** |
| 3 | 서비스 상세 첫 화면 | 설명 글이 좁은 세 줄로 접히고, 마지막 줄에 「록.」한 글자만 남았다. | `.page-hero--tall .svc-desc { max-width: 52ch }`.<br>465px 짜리 열인데 담는 칸은 1600px 이라 **29%** 만 썼다. | `max-width: 52ch → 72ch`, `text-wrap: pretty` 추가 | 6개 서비스 전부 **3줄 → 2줄**<br>외톨이 줄 5건 → **1건**(audit-advisory, 115px = 18%) |
| 4 | `about-opening.tsx` | 린트 에러 1건 (앞 세션이 넣은 것) | 같은 사이트 안 이동인데 `<a href="/services">` 를 썼다. 페이지를 통째로 다시 받는다. | `<Link>` 로 바꿨다. **주소는 그대로**. | 린트 에러 −1 |
| 5 | `page.tsx` | 린트 에러 2건 (전부터 있던 것) | `<a href="/services">`, `<a href="/blog">` | `<Link>` 로 바꿨다. **주소는 그대로**. | 린트 에러 −2 |
| 5-b | 서비스 상세 첫 화면 | 제목 밑 파란 줄과 설명 글이 **0px** 로 맞붙어 있었다 | `.svc-desc` 의 위 여백이 `@media (max-width:768px)` 안에만 있었다. 넓은 화면에는 아무 값도 없어 0 이었다. | `.page-hero--tall .svc-desc` 에 `margin-top: 1.4rem` | 선→설명 <b>0 → 21px</b><br>여덟 쪽 훑어 같은 증상 더 없음 |
| 5-c | 홈 첫 화면 로고 | 어두운 영상 위에 **검은 글씨**로 떠 있었다 | 앞 커밋에는 색을 흰→검정으로 굴리는 `logoColor`(0.11~0.2)가 있었다. 지구본 장면을 빼면서 그 값도 같이 빠졌고, `.about-stage-pin` 의 검정을 그대로 물려받았다. | `about-logo-shell` 에 `color:"#fff"` 를 못 박았다. 로고는 0.32 에 다 지워지고 영상은 0.44 까지 살아 있어 흰 바탕을 만날 일이 없다. | `rgb(0,0,0)` → **`rgb(255,255,255)`**<br>휴대폰은 원래 흰색이었음 |
| 6 | `site-search.tsx` | 린트 에러 1건 (전부터 있던 것) | `useEffect(() => setCursor(0), [q])`.<br>다 그린 뒤에 또 그린다. 그 한 프레임 동안 지워진 목록의 셋째 줄이 골라진 채로 보인다. | 그리는 중에 되돌리는 방식으로 바꿨다(React 권장).<br>`if (lastQ !== q) { setLastQ(q); setCursor(0); }` | 린트 에러 −1 |

---

## 2. 앞 세션이 넣어 둔 것 — 실제로 되는지 재봤다

| 항목 | 결과 | 잰 값 |
|---|---|---|
| 홈 첫 화면에서 본초자오선 뺌 | ✅ | 영상 → 「무슨 회사인가」 두 장면만 있음 |
| 좁은 화면은 무대를 안 붙임 | ✅ | 390px 에서 `.about-flat` / `.promise-flat` 뜸. `position:sticky` 요소 **0개** |
| `.aflat-film` 이 첫 화면을 정확히 채움 | ✅ | h=844, 화면 844 → **차이 0px** |
| 첫 그림에 데스크톱 판이 스치는지 | ✅ 안 스침 | `useSyncExternalStore` 로 렌더 도중에 읽음. 첫 페인트부터 flat |
| 헤더 로고가 흰 배경에 흰 글씨로 사라지는지 | ✅ 안 사라짐 | y0 어두움/흰글씨, y900 밝음/검은글씨. 색 뒤집힘 정상 |
| 휴대폰 비교표(#vs) 숨김 | ✅ | `display: none`. 우측 앵커 띠도 휴대폰에선 통째로 `display:none` 이라 죽은 링크 없음 |
| 하단 흐림 약하게 | ✅ | 4겹 → **2겹**, 흐림 14px → 4px, 띠 96 → **52px** |
| 헤더 굴절 약하게 | ✅ (위 2번에서 한 번 더 줄임) | 휴대폰은 `glass-refract-soft` 사용 확인 |
| 스냅·브레이크 끔 | ✅ | `pointer: coarse` **true** 로 잡힘 → 끄는 분기 탐 |
| `/portal` 카드 모이기 안정화 | ✅ | 아래 3번 표 |
| CSS 중괄호 짝 | ✅ | globals 1059/1059, promo 831/831 |
| 콘솔 경고 | ⚠ 1건 (손 안 댐) | framer-motion 개발용 경고. `useScroll` 의 스크롤 통이 `<html>` 인데 `position: static` 이라 뜬다. 배포본에는 안 나온다. 고치려면 `html` 에 `position: relative` 를 줘야 해서 4,200줄 CSS 에 미치는 영향이 커 손대지 않았다 |

---

## 3. `/portal` 카드 모이기 — `.feat-rail` 과 나란히 재기

같은 방식(브라우저 실제 스크롤 제스처)으로 두 구간을 나란히 굴려 프레임마다 쟀다.

| 구간 | 34ms 넘는 프레임 | 66ms 넘는 프레임 | 뒤로 튐 | 스크롤 멈췄는데 그림만 움직인 경우 |
|---|---|---|---|---|
| `.svm` (모이기) | 28회 | 4회 | **0건** | **0건** |
| `.feat-rail` (기준선) | 56회 | 2회 | **0건** | **0건** |

「기준선처럼 안정적이어야 한다」는 요구는 숫자상 충족한다.

---

## 4. 빌드 · 린트

| | 시작(248f5ed + 앞 세션 변경) | 지금 |
|---|---|---|
| `npm run build` | 한 번도 안 돌려봤음 | **성공** (76쪽 생성, 타입 오류 0) |
| `npm run lint` | 12건 (에러 7 / 경고 5) | **7건 (에러 3 / 경고 4)** |

CSS 중괄호 짝: globals 1059/1059, promo 831/831.

남은 에러 3건은 전부 `@ts-nocheck` 다 — `promo-motion.tsx`, `schedule-popup.tsx`, `service-merge.tsx`.
지우면 타입을 처음부터 다시 붙여야 하는 큰 작업이라 **손대지 않았다.**

---

## 5. 「누구와 준비하느냐」 섹션 — 확정

사용자 지시: **모바일에서만 안 나오면 된다.** 지금 상태가 정확히 그것이라 추가 작업 없음.
`promo.css` — `@media (max-width: 900px) { #vs { display: none } }`.
우측 앵커 띠도 휴대폰에서는 통째로 `display:none` 이라 「무엇이 다른가」로 가는 죽은 링크는 없다.

---

## 6. 클라이언트 지적 열한 건 — 원인이 숫자로 잡혔나

| 지적 | 원인 특정 | 근거 |
|---|---|---|
| 모바일 2 · 윗 라인 일그러짐 | ⬤ 숫자로 | 기울기 0.62 → 0.244 px/px |
| 모바일 3 · 하단 흐림 | ⬤ 숫자로 | backdrop-filter 4겹 14px → 2겹 4px |
| 모바일 4 · 약속 답변부 짤림 | ⬤ 숫자로 | 판 720 vs 내용 933, 213px |
| 모바일 6 · 「천천히」 짤림 | ⬤ 숫자로 | 원 안 정사각형이 지름의 0.7배뿐 |
| 웹 8 · 약속 하단 짤림 | ⬤ 숫자로 | 122px · 217px 실측 |
| 추가 10 · 왜곡 줄이기 | ⬤ 숫자로 | 밀림 6.18 → 4.41px |
| 추가 · 파란 줄 붙음 | ⬤ 숫자로 | 0px → 21px |
| 모바일 1 · 넘김 끊김 | ◐ 원인 추정 | 붙는 판·backdrop 4겹·스냅. 증상 재현은 못 함 |
| 모바일 5 · 빠른 스크롤 튐 | ◐ 원인 추정 | `promo-motion.tsx:626` — 스크롤 멎고 170ms 뒤 최대 220px 자기가 움직이던 코드. 지금 손가락 화면에서는 꺼져 있다(`pointer:coarse` 확인). 증상 자체는 못 봤다 |
| 추가 11 · portal 모이기 | ◐ 부분 | 원인 셋을 짚었고 지금 튐 0건. 원래 증상은 재현 못 함 |
| 모바일 7 · 섹션 삭제 | — 요구사항 | 원인 없음. 모바일 숨김으로 확정 |
| 웹 9 · 「후단에 나오는」 | ✗ 불명 | 문장이 끊겨 있다 |

---

## 7. 못 잰 것 — 그대로 적는다

| 항목 | 왜 못 쟀나 |
|---|---|
| 실제 아이폰에서 넘김이 부드러운가 (모바일 1·5번) | 맥의 headless Chromium 은 아이폰이 아니다. 여기서 잰 프레임 시간(7,751px 굴려서 34ms 넘는 프레임 0회)은 **참고값일 뿐**이다.<br>확실히 말할 수 있는 건 무거움의 원인이던 것들이 실제로 없어졌다는 것뿐이다 — 붙는 판 0개, backdrop-filter 4겹 → 2겹, 스냅 꺼짐 |

| 웹 피드백 9번 「후단에 나오는」 | 문장이 끊겨 있어 무엇을 말하는지 모름 |

---

## 8. 첫 화면 다시 짜기 — 밀도

고객 최초 요구: 「영상 바로 아래서 세무·회계 자문사라는 걸 알 수 있게, 정체성부터 간략히」.
그 요구를 지키면서 빈 곳을 메웠다.

| 무엇을 | 왜 | 잰 값 |
|---|---|---|
| 영상을 끄지 않고 **본문 열 폭 판**으로 줄여 화면 위에 남긴다 | 아예 끄니 다음 화면이 흰 바탕에 글 한 덩어리뿐이라 위아래로 570px 이 비었다 | 판 91~469px(378px), 좌우 29~1411 = 본문 열 |
| 글판을 판 **바로 밑**에 앉힌다 | 화면 한가운데에 두면 영상과 겹치거나 아래가 통째로 빈다 | 글판 510~761px |
| 로고를 좌우 두 칸으로 — 왼쪽 로고+업, 오른쪽 설명+단추 | 가운데 580px 만 쓰고 좌우가 비었다 | 좌우 비율 `0.82 : 1.28` — `/portal .pb-grid` 와 같은 값 |
| 로고 하나가 화면 한가운데에서 제자리로 **내려온다** | 예전에는 가운데 로고와 아래 로고가 따로 있어 하나가 꺼지고 하나가 켜졌다 — 둘로 보였다 | 시작 (720,447) 폭 675 → 끝 (226,609) 폭 394. 색 흰 → 검정 |
| 「세무 · 회계 자문」 굵게 | 고객이 제일 먼저 말한 한 줄인데 로고 밑 부제처럼 스쳤다 | 14.25px/600 → **24px/800** (휴대폰 14.25px/800) |
| 첫 화면이 화면 가운데를 잡고 있는 동안 우측 앵커 띠를 숨긴다 | 띠(x1337~1440)가 영상 판(~x1411) 위에 반쯤 걸쳐, 어느 색을 골라도 한쪽이 안 읽힌다 | y1335 부터 뜬다 |

세로 밀도(글·그림만, 헤더 제외):

| | 고치기 전 | 지금 |
|---|---|---|
| 평균 글 채움 (1440×900) | 49% | **61%** |
| 첫 화면 둘째 판 y450 / y900 | 31% / 30% | **100% / 98%** |
| y1350 | 34% | **73%** |
| 화면마다 제일 큰 빈 띠 | 226px | **160px** |
| 휴대폰 평균 | 52% | 52% (안 건드림) |

약속 구간 짤림은 네 화면 전부 **0건 유지**. 빌드 통과, 린트 7건 그대로, CSS 중괄호 짝 맞음.

아직 헐렁한 곳 — y6750 인사이트 23%, y1800 약속 시작 34%, y7200 마무리 36%.
