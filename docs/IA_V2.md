> 쉬운 요약: docs/memory/memory_IA_V2.md

# 메리디안 정보구조 다시 짜기 (v2)

작성 2026-09-05 · 잰 대상 `feat/menu-ia` (localhost:3200) · 라이브 비교 대상 `main`
이전 문서 `docs/IA_MENU_PLAN.md` 는 메뉴 여섯 칸만 손보는 안이었다. 그걸 대체한다.

---

## 0. 한 줄

지금은 **한 페이지가 여러 일을 하고, 같은 내용이 세 군데에 있고, 최종 페이지는 3단 아래 숨어 있다.** 페이지마다 일을 하나씩만 주고, 최종 페이지를 두 번 안에 닿게 만든다.

---

## 1. 지금 상태 — 잰 값

브라우저에서 직접 잰 값이다. 「내부 링크」는 그 페이지 본문에서 다른 페이지로 나가는 서로 다른 주소의 수다.

| 페이지 | 높이 | 섹션 | 제목 | 나가는 링크 |
|---|---|---|---|---|
| `/` | 12,053px | 8 | 22 | 15 |
| `/portal` | **19,682px** | 10 | 19 | **1** |
| `/services` | 7,730px | 5 | **3** | 7 |
| `/services/[slug]` | 3,735px | 2 | 9 | 9 |
| `/blog` | 4,662px | 2 | 2 | 51 |
| `/pricing` | 4,233px | 3 | 10 | **1** |
| `/clients` | 4,149px | 3 | 4 | 6 |
| `/about` | 3,277px | 5 | 4 | 8 |
| `/contact` | 2,910px | 3 | 3 | 0 |
| `/members` | 2,188px | 2 | **1** | **0** |

---

## 2. 꼬인 데 일곱

### ① 「서비스」를 누르면 서비스가 안 나온다

`/services` 는 7,730px 인데 제목이 셋뿐이다. 가운데를 **붙잡는 스크롤**(sticky)이 차지하고 있어서, 서비스 여섯 개를 **한 번에 하나씩** 스크롤로 넘겨야 본다. 훑을 수가 없다.

그런데 같은 여섯 개의 진짜 설명은 그 아래 `/services/[slug]` 여섯 장에 따로 있다. 그쪽이 오히려 알차다 — 3,735px 에 제목 9개(서비스 상세 · 산출물 · 활용 예시 · 적용 케이스).

**같은 여섯 개를 세 번 다룬다.** 홈 카드 → `/services` 붙잡는 스크롤 → 상세 6장.

### ② 최종 페이지가 3단 아래에 있다

메뉴 → `/services` → `/services/세무-기장`. 세 번 눌러야 진짜 내용이 나온다. mstacc 은 드롭다운에서 바로 상세로 간다 — 두 번.

### ③ 막다른 페이지가 셋

| 페이지 | 크기 | 나가는 링크 |
|---|---|---|
| `/portal` | 19,682px | 1개 |
| `/pricing` | 4,233px | 1개 |
| `/members` | 2,188px | **0개** |

들어가면 뒤로가기 말고는 나갈 데가 없다. `/members` 는 제목이 하나뿐이라 읽을 것도 거의 없다.

### ④ `/portal` 이 홈보다 길다

19,682px. 홈(12,053px)의 1.6배, 사이트에서 제일 길다. 이건 서비스가 아니라 **도구 소개**인데 분량이 회사 전체보다 많다.

### ⑤ 홈이 다른 페이지 몫까지 산다

홈 하나에 제목 22개다. 약속 · 지켜가는 방식 · 대시보드 · 서비스 6개 · 비교표 · 대표 회계사 · 인사이트 · 상담이 다 있다. 그리고 그 대부분이 하위 페이지에도 또 있다.

홈만 보고 나가는 사람이 제일 많으니 홈이 두꺼운 건 맞다. 문제는 **어느 쪽이 원본인지 정해져 있지 않다**는 것이다.

### ⑥ 페이지마다 이동 장치가 다르다

| 화면 | 그 안의 이동 장치 |
|---|---|
| 홈 | 우측 세로 앵커 9칸 |
| `/services` | 붙잡는 스크롤의 진행 표시 |
| `/portal` | 붙잡는 구간 여러 개 |
| `/pricing` | 6단계 위저드 |
| 나머지 | 없음 |

상단 메뉴 말고 페이지마다 다른 장치가 하나씩 더 붙는다. 어디서 무엇으로 움직여야 하는지 매번 다시 배워야 한다.

### ⑦ 가려 둔 페이지 둘

`robots.ts` 가 `/pricing` 과 `/preview` 를 막는다. 라이브에도 그대로 나간다. `/pricing` 은 원래 외부 배포본(`pricemodule-gules.vercel.app`)이던 걸 본 도메인으로 옮긴 것이고, 옮기면서 메뉴에 안 넣고 robots 로 막았다. **사고가 아니라 결정이다.**

---

## 3. 다시 짤 때 지킬 것 넷

1. **두 번 안에 닿는다.** 상단 메뉴에서 최종 페이지까지 두 번 이내.
2. **한 페이지는 한 가지 일만 한다.** 안내하는 페이지와 설명하는 페이지를 섞지 않는다.
3. **같은 내용은 한 군데가 원본이다.** 나머지는 요약 + 링크. 어느 쪽이 원본인지 문서에 적는다.
4. **페이지 안 이동 장치는 하나만.** 붙잡는 스크롤을 쓰면 앵커는 안 쓴다. 반대도 같다.

---

## 4. 새 구조

### 4-1. 상단 여섯 칸

| 칸 | 라벨 | 누르면 | 드롭다운 |
|---|---|---|---|
| 1 | 메리디안 | `/about` | 회사 소개 · 회계사 소개 · 사무소 정보 |
| 2 | 서비스 | `/services` | **상세 6장 바로** (정기 3 / 사안별 3) |
| 3 | 대시보드 | `/portal` | — |
| 4 | 고객사 | `/clients` | — |
| 5 | 인사이트 | `/blog` | 전체 · 법인세 · 부가가치세 · 소득세 · 세무일반 |
| 6 | *(수임료)* | `/pricing` | — · **7장 참조** |
| CTA | 기장 문의하기 | `/contact` | — |

서비스 드롭다운이 **중간 페이지를 건너뛴다.** 이것 하나로 3단이 2단이 된다.

### 4-2. 페이지마다 하는 일 하나

| 페이지 | 하는 일 | 원본으로 갖는 것 | 안 하는 일 |
|---|---|---|---|
| `/` | 안내 | 자오선 히어로 · 약속 | 설명. 전부 링크로 넘긴다 |
| `/services` | **목록** | 1년의 흐름 | 붙잡는 스크롤 — 없앤다 |
| `/services/[slug]` | **설명** | 서비스 상세 · 산출물 · 케이스 | — |
| `/about` | 회사가 뭔지 | 이름의 뜻 · 사무소 정보 | 회계사 이력 |
| `/members` | **회계사** | 이력 · 자격 · 전문 분야 | 회사 소개 |
| `/clients` | 단계별 자리 | 대표 3단계 | 서비스 설명 |
| `/portal` | 도구 | 대시보드 | 회사 소개 |
| `/blog` | 글 | 글 54편 | — |
| `/contact` | 받는 곳 | 폼 · FAQ | — |
| `/pricing` | 값 | 계산기 | — |

### 4-3. 손대야 하는 것

**① `/services` 에서 붙잡는 스크롤을 뺀다**

여섯 개 목록과 「1년의 흐름」만 남긴다. 7,730px → 3,000px 안쪽. 같은 여섯 개를 세 번 다루던 걸 두 번으로 줄인다(홈 카드 + 상세).

**② `/portal` 을 반으로**

19,682px 는 도구 하나에 과하다. 홈 배너에서 이미 무엇인지 보여주므로, 여기서는 「무엇이 되나 · 어떻게 시작하나」 둘만 남긴다. 목표 9,000px.

**③ `/members` 를 채운다**

지금 제목이 하나다. `data.ts` 에 이력·자격·전문 분야 6개·경력이 다 있는데 화면에서 거의 안 쓴다. 그걸 펴고, 홈 Principal 의 「프로필 전체 보기」가 여기로 온다.

**④ 막다른 페이지에 나갈 문을 낸다**

| 페이지 | 붙일 문 |
|---|---|
| `/portal` | 서비스 목록 · 수임료 · 상담 |
| `/members` | 서비스 목록 · 상담 |
| `/pricing` | 서비스 목록 · 상담 |
| `/clients` | 각 단계에서 그 단계에 붙는 서비스 상세로 |

**⑤ 이동 장치를 하나로**

홈만 우측 앵커를 갖는다. 다른 페이지는 상단 메뉴로만 움직인다. `/services` 의 붙잡는 스크롤은 ①에서 없어지고, `/portal` 의 붙잡는 구간은 ②에서 줄어든다.

---

## 5. 클릭 거리 — 전후

| 가고 싶은 곳 | 지금 | 새 구조 |
|---|---|---|
| 세무 기장 상세 | 3번 | **2번** |
| 회계사 이력 | 2번 (그리고 내용이 없음) | 2번 (내용 있음) |
| 수임료 | **못 감** (주소 직접 입력) | 7장에서 결정 |
| 대시보드 | 2번 | 2번 |
| 법인세 글만 | 2번 + 화면 안에서 거르기 | 2번 (주소에 남음) |

---

## 6. 주소는 안 바꾼다

지금 주소를 하나도 안 바꾼다. 검색 순위가 끊긴다. 링크와 페이지 안 구성만 바꾼다.

---

## 7. 정해야 하는 것 하나 — `/pricing`

가려 둔 게 결정이었으므로 되돌리려면 결정이 필요하다.

| 안 | 메뉴 | 사이트맵 | robots | 결과 |
|---|---|---|---|---|
| 그대로 가린다 | 없음 | 없음 | 막음 | 상담 때 링크로만 건넨다. 여섯째 칸이 빈다 |
| 사람에게만 연다 | **있음** | 없음 | 막음 | 눌러서 갈 수 있고 검색에는 안 걸린다 |
| 다 연다 | 있음 | 있음 | 푼다 | 값을 먼저 보여주는 쪽으로 방향 전환 |

**가린 이유를 모르면 「사람에게만 연다」가 되돌리기 쉽다.**

지금 `feat/menu-ia` 는 메뉴·푸터·검색·사이트맵에 다 넣어 둔 상태라 robots 와 어긋나 있다. 어느 쪽이든 정해서 맞춰야 한다.

---

## 8. 하는 순서

| 단계 | 하는 일 | 크기 |
|---|---|---|
| 1 | 서비스 드롭다운이 상세로 바로 (3단 → 2단) | 이미 됨 |
| 2 | `/services` 붙잡는 스크롤 제거 | 반나절 |
| 3 | 막다른 페이지 넷에 문 내기 | 반나절 |
| 4 | `/members` 채우기 | 반나절 |
| 5 | `/portal` 반으로 | 하루 |
| 6 | `/pricing` 결정 반영 | 한 시간 |
